# ไม่ต้องใส่ถ้ารัน Web + backend (Docker) บนเครื่องเดียวกัน — ใช้ localhost อัตโนมัติ
#
# ต้องการ override ทุกแพลตฟอร์ม (เช่น มือถือจริงชี้เครื่อง dev):
# EXPO_PUBLIC_API_URL=http://YOUR_PC_IP:5000
#
# ต้องการ override เฉพาะพอร์ตบนเครื่องเดียวกัน:
# EXPO_PUBLIC_API_URL=http://localhost:5000
